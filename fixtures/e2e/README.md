# Example browser checks

These source files demonstrate browser-test structure for documentation analysis.
They are inert fixtures; no dependency installation or browser session is needed.

The login test uses a page object. The health test calls a relative endpoint.
The configuration supplies a local example base URL.
