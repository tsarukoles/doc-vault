# Example operations reference

Use the handler input's `controls` field to select rule names. Unknown names
currently raise an error from the registry lookup. There is no retry or dead
letter implementation in this fixture.
